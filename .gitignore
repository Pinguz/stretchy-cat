# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 交付用的 zip 是 dist/ 打包出来的产物，同样可再生成，不进版本库
# （npm run build 后 cd dist && zip -r -X ../tool.zip .）
tool.zip

# 由 scripts/build-audio.mjs 从 media/audio 生成，不进版本库
# （改了源文件重跑 npm run audio:build，predev / prebuild 也会自动跑）
public/audio/sfx-data.js

# 本地环境变量（GEMINI_API_KEY 等）
.env
.env.*
!.env.example

# 参考素材：仅本地参考，不同步到 GitHub
伸缩猫参考素材/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
